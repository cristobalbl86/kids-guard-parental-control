{
  "name": "video-filter",
  "version": "1.0.0",
  "description": "Advanced video filtering application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "test:serial": "jest --runInBand",
    "test:core": "jest --testPathPattern=core",
    "test:filters": "jest --testPathPattern=filters",
    "test:ui": "jest --testPathPattern=ui",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "keywords": [
    "video",
    "filter",
    "content-filtering"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^14.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
